export function compute(number: number) {
    if (number < 0){
        return 0
    } 
    return number + 1;
}

// if a negative number is provided, return 0
// if a positive number is provided, return number + 1.